services:
  - type: web
    name: nafis-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python nafis_bot_web.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_IDS
        sync: false
